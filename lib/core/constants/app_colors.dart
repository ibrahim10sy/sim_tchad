import 'package:flutter/material.dart';

class AppColors {

  /// Vert agricole (couleur principale)
  static const Color primaryGreen = Color(0xFF2FAE1C);

  /// Vert institutionnel (header / menus)
  static const Color institutionalGreen = Color(0xFF0F6A2E);

  /// Vert clair (hover / highlight)
  static const Color lightGreen = Color(0xFF7ED957);

  /// Gris clair (background)
  static const Color lightGrey = Color(0xFFF4F4F4);

  /// Gris foncé (textes)
  static const Color darkGrey = Color(0xFF444444);

}